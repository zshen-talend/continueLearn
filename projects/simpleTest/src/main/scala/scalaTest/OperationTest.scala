package scalaTest

class OperationTest {
  
}

object OperationTest {
    
def main(args: Array[String]): Unit = {
  //连接两个字符串
  val suffixArrayColName: String = s"suffixArray_${System.currentTimeMillis()}"
     println(suffixArrayColName)
  }

 }